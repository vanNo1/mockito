package van.unittest.test.dao;

import van.unittest.test.entity.User;

public class UserDao {
    public boolean exist(String name, String password) throws Exception {
        throw new Exception("can't assessDB");
    }
}
